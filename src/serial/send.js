var SerialPort = require('serialport')
// var port = new SerialPort('COM3')

export default (params) => {
  // port.write(params.text, function (err) {
  //   if (err) return 'Error on write: ' + err.message
  //   else return 'message written'
  // })
  return 'hell'
}